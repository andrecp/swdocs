<!DOCTYPE html>
<html lang="en">

<head>
    <title>swdocs home</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style type="text/css">
        body {
            margin:40px auto;
            max-width:650px;
            line-height:1.6;
            font-size:18px;
            color:#444;
            padding:0 10px;
            background-color: #EEEEEE
        }
        h1, h2, h3 {
            line-height:1.2
        }
    </style>
</head>

<body>

<h1>Welcome to SwDocs!</h1>

{{ $length := len .SwDocs }} {{ if not (eq $length 0) }}
<section>
    <h3>Search for a SwDoc</h3>
    <form action="/search">
        <label for="swdocsearch">Name (use % for wildcard)</label>
        <input type="search" id="swdocsearch" name="swdocsearch">
        <input type="submit" value="search">
    </form>
    {{end}}
</section>

</br>

<section>
    {{ $length := len .SwDocs }} {{ if not (eq $length 0) }}
    <h3>Latest SwDocs created</h3>
    {{else}}
    <h3>No SwDocs found!</h3>
    {{end}}

    {{range .SwDocs}}
    <ul>
        <li><a href="/{{.Name}}">{{.Name}} was created on {{with .Created}}{{.ToString}}{{end}} (UTC)</a></li>
    </ul>
    {{end}}
</section>

</body>

</html>
